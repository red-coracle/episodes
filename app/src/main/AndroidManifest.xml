<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.redcoracle.episodes">

  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <application
      android:name="org.redcoracle.episodes.EpisodesApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:theme="@style/Theme.Episodes"
      android:allowBackup="true">

    <activity
        android:name="org.redcoracle.episodes.MainActivity"
        android:label="@string/app_name"
        android:launchMode="singleTop">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
        android:name="org.redcoracle.episodes.SettingsActivity"
        android:label="@string/settings_title"
        android:parentActivityName="org.redcoracle.episodes.MainActivity">
      <meta-data
          android:name="android.support.PARENT_ACTIVITY"
          android:value="org.redcoracle.episodes.MainActivity"/>
    </activity>

    <activity
        android:name="org.redcoracle.episodes.AboutActivity"
        android:label="@string/about_title" >
    </activity>

    <activity
        android:name="org.redcoracle.episodes.AddShowSearchActivity"
        android:label="" >
    </activity>

    <activity
        android:name="org.redcoracle.episodes.AddShowPreviewActivity"
        android:label="" >
    </activity>

    <activity
        android:name="org.redcoracle.episodes.ShowActivity"
        android:label=""
        android:theme="@style/Theme.Episodes.NoActionBar">
    </activity>

    <activity
        android:name="org.redcoracle.episodes.SeasonActivity"
        android:label="" >
    </activity>

    <activity
        android:name="org.redcoracle.episodes.EpisodeActivity"
        android:label="" >
    </activity>

    <service
        android:name="org.redcoracle.episodes.AutoRefreshHelper$Service" >
    </service>

    <receiver
        android:name="org.redcoracle.episodes.AutoRefreshHelper$BootReceiver"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>

    <receiver
        android:name="org.redcoracle.episodes.AutoRefreshHelper$NetworkStateReceiver"
        android:exported="true"
        android:enabled="false">
      <intent-filter>
        <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
      </intent-filter>
    </receiver>

    <provider
        android:name="org.redcoracle.episodes.db.ShowsProvider"
        android:authorities="${applicationId}.db.ShowsProvider"
        android:exported="false" >
    </provider>

  </application>
</manifest>
